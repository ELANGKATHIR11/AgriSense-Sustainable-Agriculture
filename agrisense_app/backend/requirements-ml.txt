# Optional heavy ML dependencies for Vision-Language and NLM features.
# Install these only when you plan to run ML features locally or on a GPU-enabled runner.
# Pin numpy to a TensorFlow-compatible 1.26.x release for Python 3.12
numpy==1.26.4
tensorflow==2.16.1
torch>=2.0.0
torchvision>=0.15.0
sentence-transformers>=2.2.0
transformers>=4.41.1
# Use a headless OpenCV build compatible with numpy 1.26 (avoid newer wheels that require numpy>=2.x)
opencv-python-headless==4.6.0.66
Pillow>=8.3.0

# Optional extras (install as needed):
# faiss-cpu
# accelerate